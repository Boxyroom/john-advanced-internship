import AppPagePlaceholder from '@/components/AppPagePlaceholder';

export default function SettingsPage() {
  return (
    <AppPagePlaceholder
      eyebrow="Account"
      title="Settings"
      description="Account preferences will live here after authentication is introduced."
    />
  );
}
