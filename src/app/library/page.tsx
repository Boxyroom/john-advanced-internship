import AppPagePlaceholder from '@/components/AppPagePlaceholder';

export default function LibraryPage() {
  return (
    <AppPagePlaceholder
      eyebrow="Saved"
      title="Library"
      description="A shared layout destination for saved books and progress once user data exists."
    />
  );
}
