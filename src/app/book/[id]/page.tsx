import AppPagePlaceholder from '@/components/AppPagePlaceholder';

export default function BookPage() {
  return (
    <AppPagePlaceholder
      eyebrow="Book"
      title="Book details"
      description="This route is ready for book metadata, summaries, and actions once book data is added."
    />
  );
}
