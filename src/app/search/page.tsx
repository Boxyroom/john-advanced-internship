import AppPagePlaceholder from '@/components/AppPagePlaceholder';

export default function SearchPage() {
  return (
    <AppPagePlaceholder
      eyebrow="Discover"
      title="Search"
      description="Search navigation is wired, with API-backed search results intentionally left for later."
    />
  );
}
