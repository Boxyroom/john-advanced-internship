import AppPagePlaceholder from '@/components/AppPagePlaceholder';

export default function ChoosePlanPage() {
  return (
    <AppPagePlaceholder
      eyebrow="Subscription"
      title="Choose plan"
      description="Plan selection layout is in place without payment logic or checkout behavior."
    />
  );
}
